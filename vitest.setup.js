import '@testing-library/jest-dom';

// Charge les matchers supplementaires AVANT chaque test :
// toBeInTheDocument() -> verifie qu'un element est dans le DOM
// toHaveValue('x') -> verifie la valeur d'un input
// toBeDisabled() -> verifie qu'un element est desactive
// toHaveTextContent() -> verifie le texte d'un element
